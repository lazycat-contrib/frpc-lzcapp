package: community.lazycat.app.frpc
version: 0.70.1
name: frpc
description: "frp client with LazyCat-managed Web UI access and persistent dynamic proxy state."
author: fatedier
license: Apache-2.0
homepage: https://github.com/fatedier/frp
min_os_version: 1.5.0

permissions:
  required:
    - net.internet

locales:
  zh:
    name: frpc
    description: "带 LazyCat 管理 Web UI 访问和动态代理持久化能力的 frp 客户端。"
  en:
    name: frpc
    description: "frp client with LazyCat-managed Web UI access and persistent dynamic proxy state."
